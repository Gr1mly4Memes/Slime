--- a/net/minecraft/world/entity/ai/gossip/GossipContainer.java
+++ b/net/minecraft/world/entity/ai/gossip/GossipContainer.java
@@ -215,6 +_,44 @@
         public void remove(final GossipType type) {
             this.entries.removeInt(type);
         }
+
+        // Paper start - Add villager reputation API
+        private static final GossipType[] TYPES = GossipType.values();
+
+        public com.destroystokyo.villager.entity.com.destroystokyo.paper.Reputation asReputation() {
+            Map<com.destroystokyo.villager.entity.com.destroystokyo.paper.ReputationType, Integer> map = new java.util.EnumMap<>(com.destroystokyo.villager.entity.com.destroystokyo.paper.ReputationType.class);
+            for (Object2IntMap.Entry<GossipType> type : this.entries.object2IntEntrySet()) {
+                map.put(toApi(type.getKey()), type.getIntValue());
+            }
+
+            return new com.destroystokyo.villager.entity.com.destroystokyo.paper.Reputation(map);
+        }
+
+        public void assignFromReputation(com.destroystokyo.villager.entity.com.destroystokyo.paper.Reputation rep) {
+            for (GossipType type : TYPES) {
+                com.destroystokyo.villager.entity.com.destroystokyo.paper.ReputationType api = toApi(type);
+
+                if (rep.hasReputationSet(api)) {
+                    int reputation = rep.getReputation(api);
+                    if (reputation == 0) {
+                        this.entries.removeInt(type);
+                    } else {
+                        this.entries.put(type, reputation);
+                    }
+                }
+            }
+        }
+
+        private static com.destroystokyo.villager.entity.com.destroystokyo.paper.ReputationType toApi(GossipType type) {
+            return switch (type) {
+                case MAJOR_NEGATIVE -> com.destroystokyo.villager.entity.com.destroystokyo.paper.ReputationType.MAJOR_NEGATIVE;
+                case MINOR_NEGATIVE -> com.destroystokyo.villager.entity.com.destroystokyo.paper.ReputationType.MINOR_NEGATIVE;
+                case MINOR_POSITIVE -> com.destroystokyo.villager.entity.com.destroystokyo.paper.ReputationType.MINOR_POSITIVE;
+                case MAJOR_POSITIVE -> com.destroystokyo.villager.entity.com.destroystokyo.paper.ReputationType.MAJOR_POSITIVE;
+                case TRADING -> com.destroystokyo.villager.entity.com.destroystokyo.paper.ReputationType.TRADING;
+            };
+        }
+        // Paper end - Add villager reputation API
     }
 
     private record GossipEntry(UUID target, GossipType type, int value) {
